<script>
	import '../app.pcss';
	import { AppShell } from '@skeletonlabs/skeleton';
</script>

<AppShell>
	<!-- (header) -->
	<!-- (sidebarLeft) -->
	<!-- (sidebarRight) -->
	<!-- (pageHeader) -->
	<!-- Router Slot -->
	<slot />
	<!-- ---- / ---- -->
	<!-- (footer) -->
</AppShell>
